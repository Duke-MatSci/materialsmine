If your installs fails consider either of the following:

- optional (fallback): `brew install libxml2` # provides `xmllint` on macOS
- requires Java (for xsd-schema-validator): e.g. `brew install temurin`
